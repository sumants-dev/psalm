# Logging Database

The logging database is a postgres database that stores the logs of the santized PII prompts and context and responses from OpenAI. Have a overall view of how your AI is performing and what it is saying to your users.