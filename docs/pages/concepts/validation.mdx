import { Callout } from 'nextra-theme-docs'

# Validation

<Callout type="warning" emoji="🏗️">
    Under Construction. We are still building this module out.
</Callout>

It often common to coerce GPT to send structured responses (XML, JSON, etc.). However, GPT is not deterministic and can often return malformed structures. It is important in your AI layer to validate malformed responses, and to handle them appropriately.